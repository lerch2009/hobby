#include <stdio.h>
#include <string.h>

int main(int argc, char const *argv[])
{
	const char *eval_str = "UPD sender port\0";
	int port
	for (int i = 0; i < strlen(eval_str); ++i)
	{
		port_number += eval_t_number = 0;
str[i];
	}


tf("The evaluated port number for %s is %d\n", eval_str, port_number );

	return 0;
}